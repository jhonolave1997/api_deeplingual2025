# =========================================
# 🔧 API DeepLingual - Variables de Entorno
# =========================================
# Copia este archivo como .env y rellena tus valores

# === Airtable Configuration ===
AIRTABLE_API_KEY=keyXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXX
AIRTABLE_TABLE_NAME=Pedagogical Outputs
AIRTABLE_LOGS_TABLE_NAME=Event Log

# === WordPress Configuration ===

# URL de tu WordPress (sin barra final)
WP_URL=https://tu-wordpress.com

# Token JWT inicial (puedes generarlo manualmente o se renovará automáticamente)
WP_JWT=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...

# === 🆕 Credenciales para Renovación Automática de JWT ===
# IMPORTANTE: Estos campos son OPCIONALES pero RECOMENDADOS
# Si no los configuras, el sistema usará solo WP_JWT (sin renovación automática)

# Usuario de WordPress (con permisos de publicación/edición)
WP_USERNAME=tu_usuario_wp

# Contraseña de WordPress o Application Password
# RECOMENDADO: Usa Application Password en lugar de tu contraseña principal
# Para generar: WordPress Admin → Usuarios → Tu Perfil → Application Passwords
WP_PASSWORD=tu_contraseña_o_app_password

# === OpenAI Configuration ===
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# === Email Configuration (Mandrill) ===
# API Key de Mandrill para envío de correos
# Obtén tu API Key en: https://mandrillapp.com/settings
MANDRILL_API_KEY=tu_mandrill_api_key_aqui

# Email del remitente
EMAIL_FROM=no-reply@tu-dominio.com

# Destinatarios para correos de plantilla requerida (separados por comas)
EMAIL_TO_TEMPLATES=email1@ejemplo.com, email2@ejemplo.com

# === Optional: Logging ===
AGENT_LOG_DIR=/tmp/logs

# =========================================
# 📖 Notas de Configuración
# =========================================

# 1. SEGURIDAD:
#    - NUNCA subas este archivo a Git (debe estar en .gitignore)
#    - Usa contraseñas fuertes
#    - Considera usar Application Passwords en lugar de contraseña principal

# 2. RENOVACIÓN AUTOMÁTICA DE JWT:
#    - Si configuras WP_USERNAME y WP_PASSWORD, el sistema renovará 
#      automáticamente el JWT cuando expire (401/403)
#    - Si NO los configuras, usará solo WP_JWT estático
#    - Ver documentación completa en: docs/wp-auth-setup.md

# 3. PLUGINS REQUERIDOS EN WORDPRESS:
#    - JWT Authentication for WP REST API (recomendado)
#      O
#    - Simple JWT Login
#    - ACF (Advanced Custom Fields)
#    - WP Stateless (opcional, para Google Cloud Storage)

# 4. OBTENER WP_JWT INICIAL:
#    curl -X POST https://tu-wp.com/wp-json/jwt-auth/v1/token \
#      -H "Content-Type: application/json" \
#      -d '{"username":"tu_usuario","password":"tu_contraseña"}'

# 5. SETUP RÁPIDO:
#    cp env.template .env
#    # Edita .env con tus credenciales
#    node test-routing-logic.js





















